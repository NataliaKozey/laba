filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/nginx/*.log

outputs.logstash:
  hosts: ["3.138.105.41:5044"]

filebeat.config.modules:
  enabled: true
  reload.enabled: false
  path: /etc/filebeat/modules.d/*.yml

setup.ilm.overwrite: true